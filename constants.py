class Constants:
    # size
    size = 16
    scale = 2
    width = size * scale * 30
    height = size * scale * 20

    # display
    window = (width, height)
    flags = 0
    depth = 32

    # files
    image_path = 'images'
    sound_path = 'sounds'
